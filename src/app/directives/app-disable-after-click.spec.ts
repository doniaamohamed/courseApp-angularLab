import { AppDisableAfterClick } from './app-disable-after-click';
import { ElementRef } from '@angular/core';
describe('AppDisableAfterClick', () => {
  it('should create an instance', () => {
    //  const elementRefMock = new ElementRef(document.createElement('button'));
    // const rendererMock = jasmine.createSpyObj('Renderer2', ['setProperty']);
  //   const directive = new AppDisableAfterClick({ nativeElement: {} } as ElementRef,
  // jasmine.createSpyObj('Renderer2', ['setProperty']) );
    expect(true).toBeTruthy();
  });
});
